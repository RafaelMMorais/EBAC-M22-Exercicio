{
  "name": "ebac-mod22-exercicio",
  "version": "1.0.0",
  "main": "index.js",
  "scripts": {
    "test": "npx cypress run",
    "cy:run": "npx cypress run",
    "report": "npm run mergeJsonReport && npm run generateReport",
    "mergeJsonReport": "npx mochawesome-merge ./mochawesome-report/*.json -o result.json",
    "generateReport": "npx merge ./mochawesome-report/result.json -o ./mochawesome-report"
  },
  "author": "",
  "license": "ISC",
  "description": "",
  "dependencies": {
    "cypress": "^13.14.2",
    "cypress-html-reporter": "^11.0.0",
    "mochawesome": "^7.1.3",
    "mochawesome-merge": "^4.3.0"
  },
  "devDependencies": {
    "@reportportal/agent-js-cypress": "^5.4.0"
  }
}
